
ibm.mq:
  queueManager: queue_manager
  channel: DEV.ADMIN.SVRCONN
  connName: localhost(1414)
  user: admin
  password: passw0rd
spring.jms.template:
  receive-timeout: 1s

## Enable to see the MQ Connection and Sessions being cached.
#logging:
#  level:
#    ROOT: DEBUG
spring:
  main:
    allow-circular-references: true